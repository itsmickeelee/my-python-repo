# Demo

A = 'hello world'
print(A)

# write a program that adds numbers

def add_nums(a,b,c):
    add_nums = a + b + c
    A = []
for num in add_nums:
    A.append(num)
    return A
print(add_nums([10+20+30]))